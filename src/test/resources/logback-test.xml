<configuration>

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Karate feature logs -->
    <logger name="com.intuit.karate" level="INFO" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>

    <!-- Karate HTTP logs (requests/responses) -->
    <logger name="com.intuit.karate.http" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT"/>
    </logger>

    <!-- Karate driver / Chrome logs -->
    <logger name="com.intuit.karate.driver" level="WARN" additivity="false" />
    <logger name="com.intuit.karate.shell" level="WARN" additivity="false" />

    <!-- Third-party noisy libraries -->
    <logger name="io.netty" level="WARN"/>
    <logger name="com.jayway.jsonpath" level="WARN"/>
    <logger name="org.thymeleaf" level="WARN"/>
    <logger name="org.slf4j" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>

    <!-- Silence everything else -->
    <root level="WARN">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
